import { toFloatStr, fromNumber } from './float'

export const floatCharWidth = 32
export const somePxls = 28
export const defaultState = { input: toFloatStr(true)(fromNumber(true)(Math.PI)), f64: true }
